typedef struct linkedlist {
    int value;                          // stores the value of the node;
    
    struct linkedlist *address;;        // stores the address of the next node;

}LinkedList;                            // Naming the structure